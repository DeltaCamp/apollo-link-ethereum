{
  "private": true,
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.9.0"
  },
  "scripts": {
    "test": "lerna run test",
    "dist": "lerna run dist",
    "watch": "lerna run --parallel watch"
  }
}
